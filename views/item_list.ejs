<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% item_list.forEach(function(item) { %>
      <li>
        <a href="<%= item.url %>"><%= item.name %></a> (<%= item.category.name %>)
      </li>
    <% }) %>
  </ul>
</body>
</html>
